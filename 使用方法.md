# mu-repo 使用方法

## 安装

```bash
# 当前目录安装
pip install -e .

# 或从 PyPI 安装
pip install mu-repo
```

## 快速开始

### 1. 注册仓库

```bash
# 注册单个仓库
mu register path/to/repo1

# 注册多个仓库
mu register path/to/repo1 path/to/repo2

# 自动注册当前目录下所有 git 仓库
mu register --all
```

### 2. 查看已注册仓库

```bash
mu list
```

### 3. 常用操作

```bash
# 查看所有仓库状态（推荐）
mu st

# 拉取更新
mu up

# 添加所有更改
mu a

# 提交更改
mu c "提交消息"

# 添加并提交
mu ac "提交消息"

# 推送到远程
mu p

# 一次性添加、提交、推送
mu acp "提交消息"
```

### 4. 批量执行命令

```bash
# 在所有仓库执行 git 命令
mu pull
mu fetch
mu status

# 执行任意命令
mu sh ls -la
mu sh python setup.py install
```

### 5. 仓库分组

```bash
# 创建组
mu group add frontend --copy=all
mu group add backend repo:repo1,repo2

# 切换组
mu group switch frontend

# 查看组
mu group

# 重置组（使用所有仓库）
mu group reset
```

### 6. 分支操作

```bash
# 检出分支（支持模糊匹配）
mu co feature-123

# 查找分支
mu find-branch feature
mu fb feature

# rebase
mu rb
```

### 7. 差异比较

```bash
# 使用 WinMerge 比较
mu dd

# 与特定提交比较
mu dd HEAD~2
```

## 配置文件

在项目根目录创建 `.mu_repo` 文件：

```
repo=path/to/repo1
repo=path/to/repo2
serial=0
git=/path/to/git.exe
```

## 查看帮助

```bash
mu help
mu clone --help
```