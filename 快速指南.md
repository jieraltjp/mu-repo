# mu-repo 快速指南 (INTJ 版)

## 核心概念
- 批量管理多个 git 仓库
- 单条命令并行/串行执行
- 配置文件：`.mu_repo`

---

## 安装
```bash
pip install -e .
```

---

## 核心命令 (Top 10)

| 命令 | 功能 | 频率 |
|------|------|------|
| `mu register <repo>` | 注册仓库 | ⭐⭐⭐⭐⭐ |
| `mu st` | 查看所有仓库状态 | ⭐⭐⭐⭐⭐ |
| `mu up` | 更新所有仓库 | ⭐⭐⭐⭐⭐ |
| `mu acp "msg"` | 添加+提交+推送 | ⭐⭐⭐⭐⭐ |
| `mu list` | 列出已注册仓库 | ⭐⭐⭐⭐ |
| `mu pull` | 拉取所有仓库 | ⭐⭐⭐⭐ |
| `mu a` | 添加所有更改 | ⭐⭐⭐ |
| `mu c "msg"` | 提交更改 | ⭐⭐⭐ |
| `mu p` | 推送到远程 | ⭐⭐⭐ |
| `mu co <branch>` | 检出分支 | ⭐⭐⭐ |

---

## 快速工作流

### 1. 初始化 (一次性)
```bash
mu register repo1 repo2 repo3
mu list
```

### 2. 日常操作
```bash
mu st              # 查看状态
mu up              # 更新
mu acp "message"   # 一键提交推送
```

### 3. 高级操作
```bash
mu sh <cmd>        # 在所有仓库执行命令
mu group add name  # 创建仓库组
mu group switch name  # 切换组
mu dd              # 差异比较 (WinMerge)
```

---

## 完整命令映射

### 仓库管理
```bash
mu register <repo>       # 注册仓库
mu register --all        # 自动注册所有子目录
mu unregister <repo>     # 取消注册
mu list                  # 列出仓库
```

### Git 快捷键
```bash
mu st                    # git status (并行)
mu up                    # git fetch origin
mu sync / upd            # up + diff
mu a                     # git add -A
mu c "msg"               # git commit -m
mu ac "msg"              # add + commit
mu p                     # git push
mu acp "msg"             # add + commit + push
mu rb                    # git rebase origin/current
```

### 分支操作
```bash
mu co <branch>           # 检出分支 (支持模糊匹配)
mu find-branch <pattern> # 查找分支
mu fb <pattern>          # find-branch 简写
```

### 批量执行
```bash
mu <any-git-cmd>         # 在所有仓库执行 git 命令
mu sh <cmd>              # 在所有仓库执行任意命令
```

### 分组管理
```bash
mu group add <name>              # 创建组
mu group switch <name>           # 切换组
mu group rm <name>               # 删除组
mu group reset                   # 重置到所有仓库
```

### 工具命令
```bash
mu dd                # 差异比较 (WinMerge)
mu dd <commit>       # 与指定提交比较
mu fix-eol           # 修复行尾符
mu get-vars          # 查看配置
mu set-var key=val   # 设置配置
```

---

## 配置文件 (`.mu_repo`)

```
repo=path/to/repo1
repo=path/to/repo2
serial=0                    # 0=并行, 1=串行
git=/path/to/git.exe        # Git 路径
group=group1,repo1,repo2    # 仓库组
current_group=group1        # 当前组
```

---

## 进阶技巧

### 指定仓库执行
```bash
mu st repo:repo1,repo2      # 只在指定仓库执行
mu pull @repo1,repo2        # 同上
```

### 查找分支
```bash
mu co 123                   # 模糊匹配 feature/123-bug
mu fb bug                   # 查找包含 "bug" 的分支
```

### 批量执行任意命令
```bash
mu sh ls -la                # 列出所有仓库文件
mu sh python setup.py install  # 安装所有项目
```

---

## 环境变量
```bash
MU_REPO_SERIAL=1            # 强制串行执行
```

---

## 帮助
```bash
mu help
mu <cmd> --help
mu --version
```

---

## 效率提示

1. **并行执行**：默认并行，速度更快
2. **分组管理**：项目多时使用分组，减少混乱
3. **快捷命令**：记住 `st`, `up`, `acp` 三个命令覆盖 80% 需求
4. **模糊匹配**：分支名只需输入部分字符
5. **批量操作**：`mu sh` 可执行任意命令，不限于 git

---

## 故障排除

| 问题 | 解决方案 |
|------|----------|
| 命令不存在 | 检查 `pip install -e .` 是否成功 |
| 仓库未找到 | 使用 `mu list` 检查已注册仓库 |
| 并行执行冲突 | 设置 `MU_REPO_SERIAL=1` 或配置文件 `serial=1` |
| Git 路径错误 | `mu set-var git=/path/to/git.exe` |

---

## 核心逻辑

```
注册仓库 → 批量操作 → 高效管理
    ↓
分组管理 (可选) → 项目隔离
    ↓
批量执行 → 统一维护
```